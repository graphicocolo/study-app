# Udemy JavaScript 練習＆講座　第8章 ストップウォッチを作ろう

## 仕様

- 表示
  - 経過時間
- ボタン
  - スタートボタン（開始する）
  - ストップボタン（停止する）
  - リセットボタン（リセットする）

## UI

- タイトル
- 経過時間 表示は 時間・分・秒、maxは1時間まで
- スタートボタン
- ストップボタン
- リセットボタン

## 実装ステップ

1. ボタンクリックでコンソールにメッセージ表示
2. 経過時間の表示エリアをDOMで取得する
3. スタートボタンで `setInterval` を開始し、経過時間を更新表示する
4. ストップボタンで `clearInterval` を呼び出し、計測を停止する
5. リセットボタンで経過時間の表示を初期値に戻す
6. スタート中に再度スタートを押しても重複してタイマーが動かないよう制御する

## ヒント

### 経過時間を時・分・秒に変換する

```js
const diffMs = Date.now() - timeStart;
const totalSeconds = Math.floor(diffMs / 1000); // 経過時間をミリ秒から秒へ

// 時間を算出
const hours   = Math.floor(totalSeconds / 3600);
// 3600 = 60秒（1分）× 60分（1時間）で割ると1時間未満の秒数が算出
// さらにその秒数を60で割って分を出す
const minutes = Math.floor((totalSeconds % 3600) / 60);
const seconds = totalSeconds % 60;
```

- `%`（剰余演算子）は割り算の余りを返す

| totalSeconds | ÷60の商 | 余り（= seconds） |
|---|---|---|
| 45 | 0 | **45** |
| 60 | 1 | **0** |
| 75 | 1 | **15** |
| 130 | 2 | **10** |

75秒なら「1分15秒」の**15秒部分**だけが取り出せる。分や時間の分は `Math.floor` で先に取り除いているので、`%` で残った端数だけが秒の表示に使える。

- `minutes` は「3600で割った余り（1時間未満の秒数）をさらに60で割って分を出す」2段階の変換

### 1時間で止める

```js
if (totalSeconds >= 3600) {
  clearInterval(countUpId);
}
```

### 表示をゼロ埋めして 00:00:00 形式にする

```js
const pad = (n) => String(n).padStart(2, '0');
element.textContent = `${pad(hours)}:${pad(minutes)}:${pad(seconds)}`;
```

## 詰まった点

ステップ5. リセットボタンで経過時間の表示を初期値に戻す まで終わった時点で詰まった点は下記の通り

- スタートボタン連打対策
- ストップウォッチが作動中であるというのはどういう条件で判定できるのか？
  - `intervalId` をフラグにできそう
  - `startWatch()` と `stopWatch()` で `intervalId` の `null` 判定を行う 
- 一度ストップボタンを押した後、再度スタートボタンを押すと時間がリセットされてしまう
- ストップ時のカウントを保持したまま再度カウントをスタートさせるにはどうしたら良いか